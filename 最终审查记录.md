# 最终打印版审查记录 - 字体优化最终版

本轮在上一版“赛时可用性 + API 审计 + 内容精简”的基础上，只做最后一轮印刷视觉收尾，目标是 **下午直接送印**。

## 最终结果

- **原封面原样保留**：新 PDF 第 1 页与上一最终版封面在同分辨率渲染下像素差异为 0。
- **A4 打印版**：最终共 **320 页**，非扫描 PDF、无加密，可正常解析。
- **正文可读性提升**：正文略增字号与行距，降低长时间翻阅时的疲劳；没有为了压页数继续挤排版。
- **章节字体重做**：目录、章节、小节采用楷体做层级强调，正文继续使用更稳定的宋体，保持蓝白个人视觉风格而不变成“教材排版”。
- **C++ 代码字体重做**：ASCII 代码改用 `Noto Sans Mono` / `Noto Sans Mono SemiBold`；中文注释自动切回 CJK 字体，避免缺字；语法高亮继续保留。
- **行内 API 更像 API**：反引号中的英文函数名、变量、复杂度记号改用等宽字体，不再和正文混在一起。
- **高频标题视觉层级增强**：模板标题、`不会用就照抄`、`API / 入口函数`、`核心逻辑`、`改板时先认` 的字形和边框层级进一步统一。
- **正文内容未重新洗牌**：这轮不改已经审定的算法逻辑和 API 内容，避免打印前最后时刻引入新 bug。

## 上一轮保留成果

- 复杂模板优先给“不会用就照抄”，再给 API 参数 / 返回值 / 索引约定。
- 静态主席树与动态区间顺序统计明确区分，避免拿错板子。
- `*_impl / dfs / push / pull / clone` 默认视为内部实现。
- 已精简自动伪调用、来源占位、无信息量接法套话和超长静态目录。
- DSU、Fenwick、ST、主席树、动态树套树、LCA、Dijkstra、Dinic、KMP、Trie、AC、Manacher、PBDS、矩阵、NTT、Li Chao 等高频接口已人工复核。

## PDF 预检

- Page size：A4。
- Pages：320。
- Encrypted：False。
- Openable：True。
- Likely scanned：False。
- 抽查：封面、目录、主席树/API 页、KMP 页、末页均未发现裁切、重叠、乱码或代码越界。
- 字体：正文/章节/C++ 等宽字体均嵌入 PDF；原封面保留其原始内嵌字体。

## 打印时

建议：A4、双面、长边翻页、实际大小 / 100%。不要让打印店再次“智能重排版”。

## 2026-08 全量修复轮（新增）

本轮在“字体优化最终版”基础上做了一轮内容/接口修复与知识点补全，共 **325 页**。

### 修复的代码 bug

- B 章：线段树合并 `add` 引用悬垂 UB（先拷贝子节点编号再递归）；树上莫队从空骨架补全为完整实现（欧拉序 + LCA + 奇偶 toggle，含 SP10707 风格批处理）；主席树照抄示例 `v64` 笔误。
- C 章：函数图倍增 `jump` 越界（加 assert）；Bron-Kerbosch `MAXC` 80→100；KM 无完美匹配时越界（返回 -1 哨兵）。
- E 章：`or_convolution` 不补 2 的幂（补齐并新增 `and_convolution`）；`fps_pow` 的 `first*k` i64 溢出（改 i128）；`Point::operator<` 破坏严格弱序（改 sgn 双键）；`linear_inv(0)` 越界；容斥 `(1<<m)` m≥31 UB（改 `1LL`）。
- F 章：多重背包二进制拆分 int 溢出（改 i64）；区间 DP 骨架命名空间级 `INF` 与骨架重名（改 `DP_INF`）；分组背包冗余空操作行删除。
- G 章：`shortest_path_break_one_wall` 重名定义两次（显式起点版改名 `_from` 并补 `INF/dx/dy` 定义）。

### 接口/文档修复

- 补齐缺失的 API 清单：仿射线段树、线段树 Beats、区间染色、对顶堆、LCT、离线动态连通性、CDQ（含 z 离散化警告）、树上主席树、Kruskal、Euler 序两节（漏列 `dfs` 先调用）、WQS 主入口、LIS 计数、最大子段和家族、闵可夫斯基和、曼哈顿 MST。
- 重名冲突改名：B 章 `FenwickCount/FenwickCompact/FenwickDistinct/WeightedDSUExample/HistoryDSU/MoQueryBasic/MoQueryUpdate`；C 章 `FenwickRangeSum`；E 章 `exgcd_ext/inv_general_ext/mod_pow_burnside/MatVV/EPS_HP/CircleH/MecPoint`。
- 清理全书“第 X 册”旧引用、`07_dp.md`/`12_classic_examples.md` 等旧文件名、各节“改板时先认”幽灵量。
- 各节补索引约定/依赖说明/边界警告（Karp/Matrix-Tree/支配树/Gomory-Hu 索引、SubtreeSum 调用顺序、DAG 路径覆盖依赖 HK 等）。

### 知识点补全

- B 章新增「启发式合并：小集合并入大集合」（含伴随信息与树上骨架）。
- G 章新增「爬山法」「模拟退火 Simulated Annealing」（含费马点示例）。
- 确认已覆盖：A* 最短路（第 k 短路骨架，A 章）、启发式搜索（IDA*，A 章）、树上启发式合并（DSU on Tree，C 章）。

### 验证

- 全部章节代码块做了逐块编译检查（clang++ -std=c++17），剩余失败均为“照抄示例”块与跨节依赖（符合本书使用方式）。
- 新增/修改的关键模板做了功能测试：树上莫队、启发式合并、FWT OR/AND 卷积、爬山/模拟退火、`fps_pow` 溢出保护，全部通过。
- 重新构建 PDF：325 页、A4、未加密。

## 2026-08 格式升级轮（赛时五步法）

本轮把全书小节升级为“赛时五步法”标准结构，目标：赛时读题 → 理解本质（维护什么/干什么）→ 按复杂度找模板 → 照抄接入 → 按题改造。

### 格式决策

- **继续使用 Markdown**：现有 reportlab 构建管线、封面矢量、语法高亮、320 页排版全部围绕 Markdown；本书数学以行内记号为主，转 LaTeX 收益低、回归风险高。

### 内容结构升级

- **00-赛时使用说明** 重写：新增「赛时五步法」「复杂度判定表（数据规模→算法族）」「任务类型表（问什么→先想什么）」；高频题型表增加“本质（在维护什么）”列；给出新版小节的标准骨架图。
- **每节标准结构**（已应用于 18 个高频小节示范，其余小节后续按同模板批量升级）：
  - 「赛时先看」字段重构：识别→**题目信号**、作用→**本质**、复杂度→**复杂度判定**、新增**维护的量**；
  - 新增「**抄板清单（照着做就行）**」（抄哪段→构造→操作→取结果）与「**改造点（按题目改这几处）**」；
  - 代码新增中文注释：struct 头注释写明“维护的量 + 不变量”，关键步骤行内注释解释“在维护什么/为什么”。
- **已升级小节**：A 二分/离散化/定长滑窗；B DSU/Fenwick/主席树/启发式合并；C LCA/Dijkstra/Dinic；D KMP/Manacher；E 线性筛/组合数/矩阵快速幂；F 0/1 背包/LIS；G 模拟退火/交互二分。

### 构建管线升级

- `build_final_pdf.py` 字体注册改为**自动探测**（Linux 字体路径优先，找不到回退 Windows simsun/simkai/consola），Linux/Windows 双环境可直接构建。
- 新增三种标题块渲染配色：抄板清单（绿）、改造点（琥珀）、维护的量（紫）。

### 输出

- 重新构建 PDF：**333 页**、A4、未加密；全部升级小节代码块逐块编译通过（clang++ -std=c++17），逻辑零改动，仅新增注释。

## 2026-08 辅助内容质检轮（可读性与一致性）

本轮对全书"API 使用说明 / 赛时先看 / 接法 / 警告 / 典题模型"等辅助内容做完整性、可读性、可理解性审查，并允许重构删改。

### 程序化审计结论

- **API 清单完整性**：全部列出的 API 名字均能在章节代码中找到定义（无缺失）；剩余"无定义"项均为参数名/返回名描述，非问题。
- **用法指引覆盖**：全书仅 3 节无任何调用指引（通用头文件骨架、最小割建模提醒——均为自明/提醒卡；本地调试宏已补一行照抄示例）。
- **字段缺口**：5 节缺「警告」字段（Huffman、基环树、树上路径计数、双模二维滚动哈希、多串 LCS）——已全部补齐。

### 逐章文字修复（约 150 处）

- **A 章（31 处）**：BigInt/Frac 改板量误抄（cur/g 描述）、二分两节交叉引用与笔误、实数二分/三分复杂度与实际迭代次数对齐、位运算警告病句、滑动窗口半截话、可变窗口"恰好 K"表述理顺、离线 BIT 约定残码清理、A* 改板量、DLX/de Bruijn 病句等。
- **B 章（30+ 处）**：DSU 编号 0..n 矛盾、单链表/双向循环链表/后继并查集/线段树赋值/动态开点/01 Trie 的"典题模型与接法重复"删除、可撤销并查集补 snapshot/rollback、二维 BIT 补 rect_sum、莫队离散化警告与代码矛盾、树上莫队复杂度矛盾、10 处"自动机节点池"误抄改"树节点池"等。
- **C 章（13 处）**：Kruskal/网格吞并/支配树/最大权闭合图/Gomory-Hu 的改板量误抄（dsu/g/it/root 描述）、树上主席树"自动机节点池"、补 2 节警告。
- **D 章（5 处）**：PatternOccurrenceAC 的 up、PAM 的 cur、编辑距离的 up 误抄修正；2D 哈希 API 清单去掉内部助手 add、补警告；多串 LCS 补警告。
- **E 章（17 项/36 处）**：全章 36 处悬空"不会用就看这里"引用统一改顺、杜教筛溢出表述矛盾合并、Min_25/Lehmer/exLucas/Prüfer/exBSGS/FFT/XOR 分布/KD-Tree/曼哈顿系列的改板量误抄、KD-Tree API 换成真实入口、多边形警告补全、极坐标圆弧图病句等。
- **F 章（18 处）**：最大子段和"恰好 K"表述与代码对齐、矩阵链乘变量名统一、轮廓线 DP 警告与代码不符、概率/期望/树上随机游走/SMAWK/Slope Trick 的接法与典题重复重写、MDP 补 API 入口等。
- **G 章（31 处）**：镜面反射/交互二分/通信 XOR/随机摘要的改板量与 API 描述修正、STL 速查半截话、两集合异或 API 描述、GCD 图接法函数名对齐、超立方配对/交通期望/树上绝对差/位对计数/扫雷/矩阵同值/拼接字符串/Walk/填魔法/丹药/最大平均子段/括号序列等改板量误抄全部修正。

### 统一性

- 全书"改板时先认这几个量"的跨章节误抄（并查集父节点/当前弧/倍增祖先表/版本根 等）已清扫；剩余的同类描述均为对应结构真实成员。
- 未动任何代码逻辑、`####` 标题与小节结构；PDF 重建后仍 333 页。

## 2026-08 最小完整示例轮（看得懂就能抄）

针对"结构体怎么定义、函数怎么调用、传参有什么要求"的反馈，新增两个标准块并写入 00 章格式规范：

### 新块定义（每节必看）

- **「最小完整示例（先抄这一段就能跑）」**：一段"题目 + 完整 solve 代码"，固定四步标注：结构体定义（构造）→ 初始化 → 调用 → 输出；附具体样例与样例输出（数值全部人工核对过）。
- **「传参要求（照这个传不会错）」**：逐参数列出含义、下标起点（0/1）、闭/开区间、范围约束、是否需要先 build/dfs、返回类型与"返回原值还是编号"。

### 已应用（18 个旗舰模板）

- B 章：Fenwick、线段树区间加（SegLazy）、主席树、DSU、莫队区间不同数
- C 章：LCA 倍增、Dijkstra、Dinic
- D 章：KMP、双哈希、Manacher、AC 自动机 fail 树计数
- E 章：组合数预处理、矩阵快速幂
- F 章：0/1 背包、LIS
- G 章：交互二分、模拟退火（含费马点封装）

### 构建渲染

- `build_final_pdf.py` 新增两种块的专属配色：最小完整示例（红框，强调"先抄这里"）、传参要求（蓝框）。
- 00 章加入"最小完整示例长什么样 / 传参要求长什么样"两节示例说明。

### 输出

- 合并 md 同步（24855 行），PDF 重建 **339 页**；代码围栏平衡校验通过，全部示例数值核对（Fenwick 25、线段树 45/14、主席树 3、DSU、莫队 2/3、LCA 1/2、Dijkstra 5、Dinic 4、KMP 0/2/4、Manacher 4/3、AC 4/3/2、C(5,2)=10、F_10=55、背包 7、LIS 4）。
- 剩余小节（约 380 个）仍为"赛时先看 + API/接法"结构，可按同一模板批量补"最小完整示例 + 传参要求"（分批进行）。

## 2026-08 完全自检自改轮（风格标准全面统一）

对全书做了一次"完全自检 → 自查 → 自改"，使内容、风格、标准全部统一。

### Phase A：全局规范化（脚本，1425 处）

- **字段命名统一**：410 节「识别」→「**题目信号**」、「作用」→「**本质**」、「复杂度」→「**复杂度判定**」（旧命名全部替换）。
- **抄板引导语统一**：4 种变体（"把本节函数/实现/struct 整段抄走；"等）统一为「把本节 `struct/class/函数组` 整段抄到 `solve()` 上面；」。
- **标点规范**：中文后半角逗号 19 处、半角括号套中文 42 处全部转全角；API 块标题变体统一。
- 维护的量字段覆盖：408 个有代码的小节中 405 个具备「维护的量」（其余 3 个为骨架/宏/提醒卡，豁免）。

### Phase B：补全「最小完整示例 + 传参要求」（约 370 节，分 9 波并行代理）

- 覆盖范围：A 全部 40 节、B 全部 48 节、C 全部 77 节、D 全部 36 节、E 全部 71 节、F 全部 42 节、G 全部 94 节，以及 00 章（每节 = 一行题目 + 3-12 行带注释调用代码 + 样例数值 + 逐参数传参要求）。
- 每节示例的函数名/参数/下标与实际代码逐一核对；样例数值多数经手算或编译运行验证。

### Phase C：结构问题修复

- **混合块拆分**：发现并修复约 10 节"示例语句被插入定义代码块内部"的结构问题（SAM 家族、随机数、二阶差分、Pollard-Rho、Bostan-Mori、MDP 等），保证用户整段照抄可编译。
- 修复「最小完整示例」块与「不会用就照抄」块并存时的重名变量问题。

### Phase D：编译级验收（clang++ -std=c++17 按小节拼接验证）

- 编写"按小节拼接编译"验证器（定义块在前、语句块包进 solve_stub、跳过注释末行判定），逐节编译全部 408 个有代码小节。
- 结果：**0 个真实语法错误**。剩余 9 个"失败"均为**跨节依赖**（Fenwick/PBDS/DoubleHash2D/SuffixAutomaton/Point 定义在其它节，书内设计如此，各节已注明依赖关系）；2 个白名单（骨架节、MDP 示例自带局部 struct，人工确认正确）。
- 代码围栏平衡校验通过；「维护的量」重复 0；缺「警告」0。

### 输出

- 合并 md 同步（**32858 行**），PDF 重建 **449 页**、A4、未加密。

## 2026-08 最终赛时可用性审查轮（示例运行级验证）

对全书做了最后一轮"赛时可用性"审查：翻一下就看到、读一眼就看懂、写一下就会写。

### 程序化审查结果

- **格式统一复查**：正文零残留（字段命名 430 节统一为题目信号/本质/复杂度判定；正文无半角标点混用；旧册号/旧文件名引用为 0）。
- **运行级验证（核心）**：编写按小节"定义块 + 依赖节自动拼装 + 示例块 + main"的编译运行验证器，对 408 个有代码小节逐一真实编译并运行示例：
  - **260 节示例真实编译+运行通过**（程序有实际输出）；
  - 125 节按规则跳过（示例需标准输入 / 骨架与宏节 / 跨文件定位节 / 自备 LCA 设计 / PBDS 需 GCC 环境）；
  - 23 节为 Windows 工具链链接问题（clang 缺 `__int128` 除法的 `__modti3/__divti3` 符号，Linux 评测环境正常，源码无错）；
  - **真实内容错误 0 个**。

### 本轮修复的真实问题

1. **代码 bug**：Wythoff 博弈 `isqrt_u128` 的 `hi - lo + 1` 在 lo=0 时 u64 溢出导致死循环（改 u128 上取中）。
2. **示例不完整**：爬山法（pts 空越界）、莫队带修改（qs/mods 空越界）、启发式合并照抄块（BagInfo 构造笔误 + 缺 n/u/v/x）、FastScanner（与照抄块重名 fs）——全部补齐字面量/改名。
3. **样例数值错误**：多边形面积（点 (1,1) 在边界上，实际返回 1 非 2，换内部点）、整体二分（"[2,5] 第 2 小→2" 算术错，改第 1 小）、CDQ 样例行坐标与输入不一致（统一）、可撤销并查集示例调用不存在的 `same` 方法（改 `find` 比较）。
4. **跨节依赖标注**：为 30+ 小节补齐「依赖：XX 节，抄板时一起抄上」行（Fenwick、LCA、HopcroftKarp、Point 基础、FPS 核心、mod_pow/exgcd 等），格式统一。

### 输出

- 合并 md 同步（**33146 行**），PDF 重建 **453 页**、A4、未加密。
- 代码围栏平衡校验通过；正文格式复查通过。
